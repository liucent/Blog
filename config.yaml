baseURL: https://www.ariesme.com/
languageCode: en-us
title: "AriesMe"
theme: "toha"

# Allow raw html in markdown file
markup:
  goldmark:
    renderer:
      unsafe: true
  tableOfContents:
    startLevel: 2
    endLevel: 6
    ordered: false

# Enable Google Analytics
googleAnalytics: UA-134996642-1

# Enable Disqus forum
#disqusShortname: does-not-exist

# Enable global emoji support
enableEmoji: true

# Export html and json files
outputs:
  section: ["html", "json"]

# Custom parameters
params:
  # copyright
  copyright: © 2020 Copyright.

  # background image of the landing page
  background: "images/background.jpg"

  # Provide logos for your site. The inverted logo will be used in the initial
  # transparent navbar and the main logo will be used in the non-transparent navbar.
  # It will be default to the theme logos if not provided.
  logo:
    main: /assets/images/main-logo.png
    inverted: /assets/images/inverted-logo.png

  # GitHub repo URL of your site
  gitRepo: https://github.com/liucent/blog

  # specify whether you want to write blog post or not
  enableBlogPost: true

  # specify whether you want to show Table of Contents in reading page
  enableTOC: true

  # specify the list of custom menus that you want to show in the top navbar.
  # they will be separated by a divider from the main menus.
  customMenus:
  - name: Notes
    url: https://www.ariesme.com/notes/

  # Provide newsletter configuration. This feature hasn't been implemented yet.
  # Currently, you can just hide it from the footer.
  newsletter:
    enable: true

  # some information about you
  author:
    name: "Vincent Liu"
    nickname: "Vincent"
    image: "images/avatar.png"
    # give your some contact information. they will be used in the footer
    contactInfo:
      email: "hi@ariesme.com"
      #phone: "+0123456789"
    # a summary of what you do
    summary:
    - Welcome to my blog
    - I love to work with some fun projects

  # PrismJS
  prism: true
  prismStyle: "coy" # theme: okaidia, coy, solarizedlight...
  prismLanguages: ["bash", "java", "toml", "yaml", "nginx", "properties"]
  prismLineNumbers: false